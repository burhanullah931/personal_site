setInterval(function(){ ;( function ( document, window, index )
    {
                $('.ttfl').filestyle({
                    input : false,
                    buttonName : 'btn-primary'
                });
    
            
    }( document, window, 0 ));}, 2000);	